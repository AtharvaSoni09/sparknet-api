# Railway Configuration for SparkNet API
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd fire-pred-mvp-main && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "sparknet-api"

[services.env]
PORT = "8000"
